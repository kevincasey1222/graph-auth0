{
  "name": "@jupiterone/integration-template",
  "version": "0.0.0",
  "description": "A JupiterOne Integration for https://auth0.com/",
  "license": "MPL-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "j1-integration collect",
    "graph": "j1-integration visualize",
    "graph:types": "j1-integration visualize-types",
    "lint": "eslint . --cache --fix --ext .ts,.tsx",
    "format": "prettier --write '**/*.{ts,js,json,css,md,yml}'",
    "type-check": "tsc",
    "test": "jest",
    "test:env": "LOAD_ENV=1 yarn test",
    "test:ci": "yarn lint && yarn type-check && yarn test",
    "build": "tsc -p tsconfig.dist.json --declaration",
    "prepush": "yarn lint && yarn type-check && jest --changedSince master",
    "prepack": "yarn build"
  },
  "dependencies": {
    "auth0": "^2.35.0",
    "jwt-decode": "^3.1.2"
  },
  "peerDependencies": {
    "@jupiterone/integration-sdk-core": "^6.2.1"
  },
  "devDependencies": {
    "@jupiterone/integration-sdk-core": "^6.2.1",
    "@jupiterone/integration-sdk-dev-tools": "^6.2.1",
    "@jupiterone/integration-sdk-testing": "^6.2.1"
  }
}
